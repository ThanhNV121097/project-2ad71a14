# PostgreSQL connection URL injected by runtime.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP port for backend server.
PORT=8080

# Secondary port fallback used only when PORT is unset.
APP_PORT=8080
